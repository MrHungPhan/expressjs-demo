
extends ./layouts/common

block content
	main
		.banner(style='background-image: url(./img/banner.png);')
		each proCate in productsCategory
			each item in proCate 
				if (typeof(item) != 'object')
					.container
						.pr-title
							.pr-left
								a(href='')= item
							.pr-right
								a(href='')
									| Xem thêm
									i.fas.fa-angle-double-right
				else
					.products
						.container
							.row
								each i in item
									.col-sm-6.col-md-3.product
										.product-item
											.product-img
												img.img-responsive(src= i.avatar, alt='Image')
												.product-overlay
													a.btn.btn-danger(href='/products/'+ i._id.toString() , type='button') Đặt mua
											.product-content
												p= i.name

	